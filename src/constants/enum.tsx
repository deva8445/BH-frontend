export enum AUTH_SCREEN {
  LOGIN = "Login",
  SIGNUP = "SignUp",
}

export enum ROLE {
  SELLER = "seller",
  BUYER = "buyer",
  GUEST = "guest",
}

export enum MODAL_TYPE {
  AUTH = "auth",
  ADD_BOOK = "addBook",
}
